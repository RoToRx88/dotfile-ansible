---
- name: Create Vim folder
  file: path={{ ansible_env.HOME }}/.vim state=directory owner={{ username }} group={{ username }}

- name: Create undo vim folder
  file: path={{ ansible_env.HOME }}/.vim/undo state=directory owner={{ username }} group={{ username }}

- name: Create swap vim folder
  file: path={{ ansible_env.HOME }}/.vim/swap state=directory owner={{ username }} group={{ username }}

- name: Create autoload vim folder
  file: path={{ ansible_env.HOME }}/.vim/autoload state=directory owner={{ username }} group={{ username }}

- name: Get vim-plug
  get_url: url=https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim dest={{ ansible_env.HOME }}/.vim/autoload/plug.vim

- name: Upload vim configuration file
  copy: src=dot-vimrc dest={{ ansible_env.HOME }}/.vimrc owner={{ username }} group={{ username }}
